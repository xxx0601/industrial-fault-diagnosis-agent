# Web framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0

# Settings & validation
pydantic>=2.9.0,<3.0
pydantic-settings>=2.6.0,<3.0

# HTTP / uploads (used in later steps)
python-multipart>=0.0.12,<1.0

# LangGraph agent (Step 5)
langgraph>=0.2.60,<0.4
langchain-core>=0.3.0,<0.4

# RAG / Knowledge base (Step 6)
pypdf>=5.0.0,<6.0
# Optional: ChromaDB (Python 3.11–3.12 recommended on Windows)
# chromadb>=0.5.0,<0.7

# Database (optional)
# sqlalchemy>=2.0.36,<3.0
# alembic>=1.14.0,<2.0
# psycopg[binary]>=3.2.0,<4.0

# Dev & E2E (see requirements-dev.txt)
# pytest>=8.0
# requests>=2.31
